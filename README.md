Pour pouvoir exécuter l'application veuiller ouvrir un terminal dans le dossier du projet et taper les commandes suivantes:

composer install
npm install
php artisan migrate
php artisan db:seed
npm run build
php artisan serve